import type { BasicTheme } from "basic-styled";

const dark: BasicTheme = {
  palette: {
    primary: "#C5CAFC",
    secondary: "#FCC5CB",
    tertiary: "#ACDFE1",
    mainText: "#FFFFFF",
    subText: "rgb(255, 255, 255, 0.541)",
    background: "#212121"
  }
};

export default dark;
